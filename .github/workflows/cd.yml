name: cd

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  my-job:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Write echo
        run: |
          echo "this is echoworking yah boi" ${{ inputs.release }}

      - name: My Step
        id: my-step
        run: |
          echo "Received value: ${{ github.event.workflow_run.outputs.myVariable }}"
